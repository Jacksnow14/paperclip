{
  "name": "routing-scorecard-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Cost-adjusted routing/scorecard decision engine — standalone reference implementation.",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && node dist/src/server.js",
    "test": "npm run build && node --test dist/tests"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/node": "^20.12.7"
  }
}
